import { CheckoutForm } from "@/components/organisms/(forms)/CheckoutForm";
import React from "react";

const page = () => {
  return (
    <div>
      <CheckoutForm />
    </div>
  );
};

export default page;
